import {
  API_KEY,
  API_URL,
  API_URL_IMAGE,
  API_TOKEN,
} from 'react-native-dotenv'

const config = {
  apiUrl: API_URL,
  apiUrlImage: API_URL_IMAGE,
  apiKey: API_KEY,
  apiToken: API_TOKEN,
};

export default config;
